# =============================================================================
# NEXUS PLATFORM - VARIÁVEIS DE AMBIENTE (100% COMPLETO)
# =============================================================================
# Copie este arquivo para .env e preencha os valores
# =============================================================================

# -----------------------------------------------------------------------------
# BANCO DE DADOS (PostgreSQL - Prisma)
# -----------------------------------------------------------------------------
# Obrigatório: usado pelo Prisma em prisma/schema.prisma
# Formato: postgresql://usuario:senha@host:porta/nome_banco?schema=public
DATABASE_URL="postgresql://usuario:senha@localhost:5432/nexus_db?schema=public"

# -----------------------------------------------------------------------------
# REDIS (opcional - usado no Docker, não há uso direto no código atual)
# -----------------------------------------------------------------------------
# REDIS_URL="redis://localhost:6379"

# -----------------------------------------------------------------------------
# JWT - Autenticação
# -----------------------------------------------------------------------------
# Obrigatório: usado em api/auth, api/auth/verify, api/municipios/acessos, api/municipios/permitidos
JWT_SECRET="sua_chave_secreta_jwt_aqui"

# -----------------------------------------------------------------------------
# AWS S3
# -----------------------------------------------------------------------------
# Opcional (Usa IAM Role se vazio): Usado em s3Service.ts
# IMPORTANTE: Em produção (EC2) deixe as chaves abaixo vazias (usa IAM Role).
# Para desenvolvimento LOCAL, preencha com suas chaves de acesso.
AWS_ACCESS_KEY_ID=""
AWS_SECRET_ACCESS_KEY=""
AWS_REGION="us-east-1"
AWS_S3_BUCKET="projeto-nexus-aws"

# Públicas (para uso no frontend)
NEXT_PUBLIC_AWS_REGION="us-east-1"
NEXT_PUBLIC_AWS_S3_BUCKET="projeto-nexus-aws"

# -----------------------------------------------------------------------------
# GOOGLE MAPS / ROTAS
# -----------------------------------------------------------------------------
# Obrigatório para rotas: usado em api/rotas/*, mapsApiGuard
GOOGLE_MAPS_API_KEY="sua_chave_google_maps"

# Público (para mapas no frontend)
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY="sua_chave_google_maps"

# Opcional: chave separada para Google Routes Optimization API
# Se não definida, usa GOOGLE_MAPS_API_KEY como fallback
# GOOGLE_ROUTES_API_KEY="chave_especifica_routes"

# -----------------------------------------------------------------------------
# CONTROLE DE CUSTOS - Google Maps API
# -----------------------------------------------------------------------------
# Kill switch: true desabilita todas as chamadas à API
MAPS_DISABLED=false

# Limites diários (evitar custos inesperados)
MAPS_DAILY_CAP_ROUTES=1000
MAPS_DAILY_CAP_GEOCODE=1000

# -----------------------------------------------------------------------------
# APLICAÇÃO
# -----------------------------------------------------------------------------
NODE_ENV="development"
NEXT_TELEMETRY_DISABLED=1

# -----------------------------------------------------------------------------
# DOCKER / EXTERNOS (opcional)
# -----------------------------------------------------------------------------
# OSRM_API_URL="http://router.project-osrm.org"
